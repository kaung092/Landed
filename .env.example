# Copy this file to `.env` and fill in the values you need. `.env` is gitignored.

# --- Asset root -----------------------------------------------------------
# The folder the app reads/writes and that the Claude "CoWork" agent works in:
# your base résumé, tailored-resume folders, the tailor queue, and the
# instruction .md files that brief CoWork. Defaults to ./asset-root in the repo.
ASSET_ROOT=./asset-root
# Where the CoWork instruction .md files live. Defaults to $ASSET_ROOT/instructions.
# INSTRUCTIONS_ROOT=./asset-root/instructions

# --- Database -------------------------------------------------------------
# SQLite file — the single source of truth. Defaults to data/jobhunt.db.
# DB_PATH=data/jobhunt.db

# --- Anthropic ------------------------------------------------------------
# Read automatically by @anthropic-ai/sdk if any server-side Claude calls run.
# Most agentic work happens through Claude CoWork over the MCP server instead.
# ANTHROPIC_API_KEY=sk-ant-...

# --- Gmail (inbox-sync) ---------------------------------------------------
# The app reads your inbox over IMAP (read-only) with a Gmail APP PASSWORD — this powers inbox-sync
# for every agent (CoWork + headless Claude Code) via the searchGmail/getGmailThread MCP tools.
# Easiest: leave these unset and connect from the app's CoWork page (Gmail card). Or set them here to
# manage via env (env wins over the in-app value and hides the in-app form). Create an app password
# at https://myaccount.google.com/apppasswords (needs 2-Step Verification).
# GMAIL_USER=you@gmail.com
# GMAIL_APP_PASSWORD=abcd efgh ijkl mnop

# --- LinkedIn import (browser) --------------------------------------------
# The linkedin-import job (LinkedIn Scout) needs a real, logged-in browser — LinkedIn is login-gated
# and client-rendered. Set LINKEDIN_PROFILE_DIR to a persistent Chrome profile and the app wires a
# Playwright MCP (browser_navigate/snapshot/click/evaluate) into the Claude Code runner. One-time:
#   1) set LINKEDIN_PROFILE_DIR below   2) `npm run linkedin:login` and log into LinkedIn once
#   3) restart the app. Then run the LinkedIn Scout agent — it drains the queued import jobs.
# Leave unset to keep the runner browser-free (linkedin-import jobs then just wait, harmlessly).
# LINKEDIN_PROFILE_DIR=./data/linkedin-profile

# --- Optional -------------------------------------------------------------
# Base URL the MCP server uses to reach the running app. Defaults to http://localhost:3000.
# JOBHUNT_URL=http://localhost:3000
# Database backups (scripts/backup-db.mjs).
# BACKUP_DIR=data/backups
# BACKUP_KEEP=60
# One-off inbox reconcile script (scripts/reconcile-inbox.mjs).
# INBOX_AUDIT_CSV=data/inbox-audit.csv
# Prune the audit log (events table) older than N days on boot. Unset = keep forever.
# EVENTS_RETENTION_DAYS=365